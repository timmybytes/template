import tw from 'twin.macro'
import React, { FC } from 'react';
import { CustomCssProps } from '~/config';

export type {{#if kljh}}{{pascalCase name}}{{/if}}Props = CustomCssProps & {
  children?: React.ReactNode;
}

/**
 * {{pascalCase name}}
 */

export const {{pascalCase name}}: FC<{{pascalCase name}}Props> = ({
    children,
    customCss
  }) => {
  return (
    <Container>
      {children}
    </Container>
  )
}

const Container = tw.div``
